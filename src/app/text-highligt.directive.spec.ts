import { TextHighligtDirective } from './text-highligt.directive';

describe('TextHighligtDirective', () => {
  it('should create an instance', () => {
    const directive = new TextHighligtDirective();
    expect(directive).toBeTruthy();
  });
});
